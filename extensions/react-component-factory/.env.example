# React Component Factory — Environment Variables
#
# This extension uses Raycast preferences instead of .env files.
# Configure in Raycast → Extensions → React Component Factory → Preferences:
#   - Components Root: path to your components directory
#   - Default Author: your name
